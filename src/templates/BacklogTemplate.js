import React from "react";

function BacklogTemplate() {
  return (
    <div className="BacklogTemplate">
      <h2>Backlog is empty. Create some tasks.</h2>
    </div>
  );
}

export default BacklogTemplate;
